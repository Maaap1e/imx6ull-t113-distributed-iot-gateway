#!/bin/sh
case "${1:-}" in
    start) /opt/iot-gateway/scripts/start_stack.sh t113 ;;
    stop) /opt/iot-gateway/scripts/stop_stack.sh t113 ;;
    restart) "$0" stop; "$0" start ;;
    *) echo "Usage: $0 {start|stop|restart}" >&2; exit 2 ;;
esac
