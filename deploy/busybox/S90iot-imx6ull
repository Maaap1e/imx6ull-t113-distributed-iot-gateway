#!/bin/sh
case "${1:-}" in
    start) /opt/iot-gateway/scripts/start_stack.sh imx6ull ;;
    stop) /opt/iot-gateway/scripts/stop_stack.sh imx6ull ;;
    restart) "$0" stop; "$0" start ;;
    *) echo "Usage: $0 {start|stop|restart}" >&2; exit 2 ;;
esac
